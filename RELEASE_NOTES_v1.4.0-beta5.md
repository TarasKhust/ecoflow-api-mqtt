# 🚀 Release Notes - v1.4.0-beta5

**Release Date**: 2025-12-12  
**Type**: Pre-release (Beta)

---

## 🎯 Головні зміни

### 1. 🐛 Виправлення логіки інтервалу оновлення

**Проблема:**
- При підключенні MQTT інтервал REST запитів автоматично збільшувався × 2
- Користувач встановлював 15 секунд, а реально ставало 30 секунд
- UI показував неправильне значення
- Незрозуміла "магічна" поведінка

**Виправлення:**
- ✅ Видалено автоматичне збільшення інтервалу
- ✅ Користувач має **повний контроль** через налаштування
- ✅ Встановили 15 секунд = буде **рівно 15 секунд**
- ✅ Прозора і зрозуміла поведінка

**До:**
```
Конфіг: 15 секунд → MQTT підключається → Стає: 30 секунд ❌
```

**Після:**
```
Конфіг: 15 секунд → MQTT підключається → Залишається: 15 секунд ✅
```

---

### 2. 📊 Покращене логування для діагностики

Додано детальні логи для перевірки роботи гібридного режиму:

#### REST запити:
```
🔄 [14:23:15] REST UPDATE TRIGGERED for 0274
   (configured_interval=15s, actual_since_last=15.1s, mqtt=ON)
✅ [14:23:15] REST update for 0274: received 160 fields, 3 changed
📊 [14:23:15] Changed fields (3 total):
   • bmsBattSoc: 85 → 84
   • invOutputWatts: 120 → 118
   • bpBattSoc: 85 → 84
🔀 [14:23:15] Merged data: REST=160 + MQTT=250 = Total=269 fields
```

#### MQTT повідомлення:
```
⚡ [14:23:17] MQTT message for 0274: 5 fields updated
   Fields: invOutputWatts, invOutputVolt, invOutputAmp, ...
   Total MQTT fields: 250 → 253 (+3 new)
```

**Що тепер видно в логах:**
- ⏱️ Timestamp кожного запиту
- 📏 Реальний час між REST запитами (`actual_since_last`)
- 🔢 Кількість змінених полів
- 📝 **ВСІ** змінені поля (не тільки перші 20)
- 📊 Статистика REST + MQTT полів
- ⚡ Реал-тайм MQTT оновлення

---

## 🔧 Технічні зміни

### Змінені файли:

1. **`hybrid_coordinator.py`**:
   - Видалено автоматичне масштабування інтервалу (× 2)
   - Додано tracking `_last_rest_update` для перевірки інтервалу
   - Покращено логування REST запитів з timestamp
   - Додано детальне логування всіх змінених полів
   - Покращено логування MQTT повідомлень

2. **`manifest.json`**:
   - Версія: `1.4.0-beta4` → `1.4.0-beta5`

### Нова документація:

- ✅ `BUGFIX_interval_logic.md` - детальний опис виправлення
- ✅ `LOGGING_EXAMPLE.md` - приклади логів з поясненнями

---

## 📖 Як використовувати

### Налаштування інтервалу:

1. Відкрийте **Settings → Devices & Services → EcoFlow API**
2. Натисніть **Configure**
3. Виберіть **Update Interval**:
   - **5 секунд (Fast)** - для дебагу
   - **10 секунд** - часті оновлення
   - **15 секунд (Recommended)** - оптимально ✅
   - **30 секунд** - менше навантаження
   - **60 секунд (Slow)** - мінімум запитів

### Перевірка роботи в логах:

1. Відкрийте **Settings → System → Logs**
2. Фільтр: `custom_components.ecoflow_api.hybrid_coordinator`
3. Шукайте:
   - `REST UPDATE TRIGGERED` - кожні N секунд
   - `actual_since_last` - реальний інтервал (має бути ≈ налаштованому)
   - `Changed fields` - що змінилося
   - `MQTT message` - реал-тайм оновлення

---

## 🎯 Гібридний режим (REST + MQTT)

Після цього виправлення гібридний режим працює так:

- 🔄 **REST API**: кожні N секунд (з налаштувань) - 160 полів
- ⚡ **MQTT**: кожні 2-3 секунди (автоматично) - 250+ полів
- 🔀 **Результат**: до 269 унікальних полів

**Переваги:**
- ✅ Повнота даних (REST забезпечує всі поля)
- ✅ Реал-тайм (MQTT миттєво оновлює важливі поля)
- ✅ Контроль користувача (інтервал не змінюється автоматично)
- ✅ Прозорість (детальні логи)

---

## 🐛 Виправлені проблеми

- ❌ **Автоматична зміна інтервалу** → ✅ Інтервал тепер сталий
- ❌ **Неправильне відображення в UI** → ✅ UI показує реальне значення
- ❌ **Неповні логи змін** → ✅ Показує всі змінені поля
- ❌ **Немає timestamp в логах** → ✅ Додано точний час
- ❌ **Неможливо перевірити інтервал** → ✅ `actual_since_last` показує реальний час

---

## ⚠️ Breaking Changes

**Немає breaking changes!** Це виправлення вдосконалює існуючу функціональність.

**Якщо ви раніше покладалися на автоматичне збільшення інтервалу:**
- Раніше: 15s налаштування → 30s реально
- Тепер: 15s налаштування → 15s реально
- **Рекомендація**: Якщо хочете рідше опитувати API, змініть інтервал на 30s в налаштуваннях

---

## 🧪 Тестування

### Базове тестування:

1. ✅ Встановіть інтервал 15 секунд
2. ✅ Перезавантажте HA
3. ✅ Перевірте логи:
   - Має бути `actual_since_last ≈ 15.0s`
   - Не має бути "interval changed"
4. ✅ Перевірте UI - має показувати 15 секунд

### Розширене тестування:

1. ✅ Спостерігайте за MQTT повідомленнями (кожні 2-3 сек)
2. ✅ Перевірте REST запити (кожні 15 сек)
3. ✅ Змініть інтервал через UI (5/10/15/30/60)
4. ✅ Перевірте, що новий інтервал застосувався

---

## 📦 Встановлення

### Через HACS (рекомендовано):

1. HACS → Integrations → EcoFlow API
2. Update → v1.4.0-beta5
3. Restart Home Assistant

### Вручну:

1. Завантажте `ecoflow-api-v1.4.0-beta5.zip`
2. Розпакуйте в `custom_components/ecoflow_api/`
3. Restart Home Assistant

---

## 🔗 Посилання

- 📄 [BUGFIX_interval_logic.md](BUGFIX_interval_logic.md) - детальний опис виправлення
- 📄 [LOGGING_EXAMPLE.md](LOGGING_EXAMPLE.md) - приклади логів
- 🐛 [GitHub Issues](https://github.com/TarasKhust/ecoflow-api-mqtt/issues)
- 📖 [Documentation](https://github.com/TarasKhust/ecoflow-api-mqtt)

---

## 👤 Автор

**Taras Khust** ([@TarasKhust](https://github.com/TarasKhust))

---

## 📝 Changelog

### v1.4.0-beta5 (2025-12-12)

#### Fixed
- 🐛 Видалено автоматичне збільшення інтервалу REST запитів при підключенні MQTT
- 🐛 Виправлено відображення інтервалу в UI (тепер показує реальне значення)

#### Added
- ✨ Детальне логування REST запитів з timestamp
- ✨ Відстеження реального часу між запитами (`actual_since_last`)
- ✨ Логування всіх змінених полів (не тільки перших 20)
- ✨ Покращене логування MQTT повідомлень з деталями
- 📄 Додано `BUGFIX_interval_logic.md` з описом виправлення
- 📄 Додано `LOGGING_EXAMPLE.md` з прикладами логів

#### Changed
- 🔄 Інтервал REST запитів тепер завжди дорівнює налаштованому в конфігурації
- 📊 Покращено формат логів для зручності читання

---

**Дякуємо за використання EcoFlow API!** 🙏

Якщо знайдете проблеми, створіть [issue на GitHub](https://github.com/TarasKhust/ecoflow-api-mqtt/issues).

